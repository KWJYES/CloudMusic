package com.example.cloudmusic.utils.callback;

import com.example.cloudmusic.entity.SearchWord;
import com.example.cloudmusic.entity.Song;

public interface SearchWordsItemClickCallback {
    void onItemClick(SearchWord searchWord);
}
